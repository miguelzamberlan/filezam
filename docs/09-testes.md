# 09 · Testes

## Automatizados

| Suíte | Onde | Cobre | Comando |
|---|---|---|---|
| Normalização de caminhos | `internal/vfs/pathutil_test.go` | ~30 entradas maliciosas/válidas, `ValidName`, helpers | `go test ./internal/vfs/` |
| Sandbox | `internal/vfs/root_test.go` | symlinks para `/etc`, `../outside`, loop e irmão interno; canário fora do root; escopo aninhado; rename/move/copy/remove; cancelamento; partes e `Finalize`; `TestFindStaysInsideRoot` (pesquisa não segue symlinks, não vê o canário, pula partes, respeita limites); `TestTrashIsInvisibleToTreeOps`; `TestWalkEntriesStaysInsideRoot`; `TestIsEmptyRefusesEscape` (a checagem de pasta vazia do link de recebimento não sai do root e trata parte de upload como conteúdo); `TestExtractRefusesEscape` (zip forjado com `..`, caminho absoluto, symlink, nome reservado e caractere de controle: nada sai da pasta de destino, nenhum symlink é criado, canário intacto e todo caminho extraído continua endereçável); `TestExtractIgnoresDeclaredMode` (setuid/setgid do arquivo não sobrevivem); `TestExtractBombs` (cabeçalho mentindo 1 TiB não vira arquivo nem espaço reservado; teto de bytes e de entradas cortam); `TestCopyPublishesOnlyWhenComplete` (cópia cancelada não deixa arquivo nem temporário, e substituir cancelado mantém o original); `TestRemoveTempsOnlyTouchesTaggedTemporaries` (a limpeza depois de uma queda apaga só os temporários da operação, na pasta e abaixo, sem seguir symlink); `TestExtractSkipsUnreadable` (entrada cifrada não vira arquivo de lixo, método não suportado e duplicada viram aviso); `TestExtractRefusesEncryptedArchive` (zip com todo arquivo cifrado é recusado por `CheckZip` e `ExtractZip` sem escrever nada; zip misto, só de pastas e não-zip não são confundidos com ele); `TestExtractDecodesLegacyNames` (nomes de zip do Windows em CP850: ç, ã, õ, maiúsculas acentuadas); `TestZipPartsCoverEveryFileOnce` (partes contíguas, cada uma um zip válido, arquivo maior que o teto sozinho, arquivo novo entre o plano e o download cai em uma parte só, `ZipOrder` segue a ordem da varredura); `TestZipIndexIsBoundedBeforeLoading` (registro final mentindo entradas e tamanho não faz o leitor passar do índice medido, a contagem real vence a declarada, zip64 com 66 000 entradas é medido e total declarado acima do teto é recusado); `TestExtractCancels`; `TestExtractRoundTrip` (o zip que o próprio produto gera volta inteiro); `TestWalkDepthCapAndUnreadableDirs` (varredura para em `WalkMaxDepth` e pula pasta ilegível; `TestNormalizeDepth` em `pathutil_test.go`); `TestCopyIntoItselfThroughSymlink` (destino via symlink para dentro da origem termina sem recursão); `TestSortEntries`; `TestFold` (acentos, cedilha, NFD do macOS, ß/ø/ł/æ/œ, ligaduras e dígitos largos, escrita sem caixa intacta); `TestValidName` (inclui os controles bidi U+202A–U+202E e U+2066–U+2069, que desenham `nota‮gpj.exe` como `notaexe.jpg`, e mantém as marcas de direção LRM/RLM/ALM) | idem |
| Integração HTTP | `internal/server/server_test.go` | fluxo completo: admin inicial → troca forçada → CSRF → usuário com escopo → traversal → mkdir/PUT/lote/chunked (fora de ordem, idempotente, incompleto) → rename/copy/move/delete jobs → zip → favoritos → shares públicos e revogação → mudança de escopo derruba sessão → desativar/excluir usuário → auditoria; expiração de share com relógio falso; rate limit de login; cabeçalhos da SPA; `TestSecurityHardening` (cabeçalhos do conteúdo inline, nomes com caracteres de controle, leitura de partes de upload recusada, links de usuário desativado/escopo estreitado, rate limit da troca de senha); `TestLogPath`; `TestSearch` (escopo, symlink, limite, erros, sem acentos/maiúsculas em arquivo e pasta, termo só de acento recusado); `TestListPaging`; `TestListDirs` (o seletor de destino do "Mover para…" só devolve pastas, sem arquivo nem symlink, e não sai do escopo); `TestFileShareAndPassword`; `TestPublicDownloadWaitsForSlot` (acima de 2 downloads públicos por IP a requisição espera o slot e só dá 429 após o teto; teto global de zips públicos); `TestTrash` (lixeira, restauração com conflito, escopo, permanente, varredura); `TestSearchIndex` (varredura manual termina, aparece no status com a duração e marca de onde a próxima periódica conta; índice, sem acentos pelo índice, escape do LIKE, fantasmas, ganchos, reindex); `TestLockoutIsSilentAndPerIP`; `TestShareBoundToInode`; `TestNotifications` (três envios por um link viram uma notificação do dono somada; outro usuário não lê, não marca e não apaga; lida, o próximo envio abre outra; marcar todas; link revogado pela manutenção avisa o dono); `TestBrokenSharesAreRevokedAfterGrace` (item apagado ou trocado por fora marca o link, item que volta desmarca, revogação depois de 24 h auditada, sumiço em massa nunca revoga); `TestSharesFollowAppChanges` (excluir/mover/renomear pelo app revoga os links do item e dos filhos, sem pegar o vizinho de mesmo prefixo, e restaurar não os traz de volta; `shares/affected` conta os mesmos links antes, sem token, com caminho relativo ao escopo de quem pergunta); `TestTrashCrashSafety` (linha fantasma some ao restaurar; movimento recusado não deixa linha nem pasta de id); `TestPendingTrashIsRecovered` (exclusão que caiu antes de mover termina, cópia parcial entre dispositivos é completada sem temporário, movimento concluído só marca a linha, linha sem item some com a lixeira à vista e espera com a lixeira num disco ausente, exclusão do processo atual não é tocada, restaurar devolve o item inteiro e a retenção pula pendentes); `TestUploadSessionsPerUserAndReserve` (sessão exclusiva por usuário, quem finaliza primeiro grava, teto de reserva por usuário); `TestUploadSessionSupersededByAnotherFile` (envio interrompido não tranca o nome: outro arquivo com o mesmo nome — outro tamanho, ou `mtime` afastado mais de 1 s — substitui a sessão parada e apaga a parte dela; mesmo tamanho e mesmo `mtime`, ou envio sem `mtime`, continuam sendo retomada); `TestQuotaAndJobCap`; `TestJobHistoryAndMetrics`; `TestTOTPFlow` (cadastro com senha, recusa de recadastro com 2FA ativo, segunda etapa, anti-replay, dispositivo confiável, recuperação, desativar, exigência para admins, reset pelo admin, `secret.key` 0600); `TestPublicationHardening` (caminhos reservados em zip/link/favorito/dirs do admin, `X-Forwarded-For` em várias linhas, limitador por (usuário, IP), validade assinada do cookie de senha do link); carência de 15 min para partes órfãs; `TestAdminSettings` (padrão de fábrica, recurso desligado barra até o admin, teto rígido de 30 dias, usuário comum recusado, auditoria; retenção da lixeira e intervalo do índice herdam o ambiente até serem gravados, respeitam as faixas e valem na hora — a exclusão passa a usar a lixeira); `TestShareSlug` (apelido exige senha, formato e nomes reservados, duplicado, resolve por apelido e por token, **apelido continua reservado depois de revogado** e `?purge=1` o libera); `TestSlugEnumerationIsRateLimited` (varredura trava, acerto legítimo não paga, link travado não devolve o nome); `TestDropCreateRequiresEmptyFolder` (pasta criada no ato, pasta com arquivo ou com parte de upload recusada, vencimento e cota obrigatórios, nada de pasta órfã após falha); `TestDropAnonymousUpload` (PUT único e sessão em blocos sem sessão, CSRF exigido, auditoria); `TestDropNeverOverwrites` (arquivo do dono intacto, dois visitantes viram nomes distintos); `TestDropLimits` (cota, teto por arquivo, contagem, sessão aberta segura a cota e abortar devolve); `TestDropOwnerQuotaIsEnforced` (cota do dono responde `drop_full`, nunca `quota_exceeded`); `TestDropIsNotReadable` (leitura 404 nos dois sentidos, escrita travada sem unlock); `TestDropSenderCookieIsSigned` (cookie adulterado não vê envio alheio); `TestDropSlowUploadDoesNotBlockOthers` (um envio lento não pode prender os outros do mesmo link); `TestStalledTransferIsNotInternalError` (o `i/o timeout` do prazo de leitura, o contexto expirado e o corpo cortado viram 408/400/499, não 500); `TestDropSenderSurvivesParallelFirstUpload` (abrir a página já dá identidade ao visitante, e envios em paralelo não a trocam — sem isso os primeiros arquivos sumiam da lista do próprio remetente); `TestDropUploadsHiddenFromOwner` (a interface do dono não enxerga nem cancela sessão de visitante); `TestDropSurvivesRateLimit` (150 blocos seguidos sem 429); `TestDropFolderRemovedMidUpload` (link e sessões caem junto com a pasta); `TestZipDownloadInParts` (plano divide pelo tamanho, cada parte baixa como zip completo com o nome pedido e as partes cobrem cada arquivo uma vez, no logado e no link público; caminho fora do link é recusado); `TestExtractAndArchive` (pasta nova com o nome do arquivo, segunda extração vai para o lado, não-arquivo, `.zip` corrompido e `.zip` com senha são 400 síncronos sem pasta criada, teto de tamanho, interruptor do admin, e o `.zip` gerado sem temporário para trás); `TestExtractBombFailsJob` (bomba declarada acima do teto é 413 na hora, sem job nem pasta); `TestInterruptedJobsAreCleanedUp` (job que termina não deixa `job_cleanup`; depois de uma queda simulada, temporários da cópia somem e os arquivos completos ficam, a extração parcial some, temporário de outro job fica, o histórico ganha o texto certo e um destino em disco não montado espera); `TestThumbnails` (gera com o lado maior em 256, cabeçalhos de cache e ETag, 304 na revalidação, arquivo alterado gera miniatura nova, formato sem decodificador e imagem acima do teto de pixels caem em 404, cache fora da árvore do usuário, interruptor do admin); `TestPutContentIfMtime` (gravação com o mtime lido passa, com um mtime velho é recusada com `modified` e o conteúdo de quem salvou antes sobrevive; sem o parâmetro, sobrescreve como sempre); `TestDropDoesNotLeakExistingNames` (nome que existe e nome que não existe respondem igual, no PUT e na sessão em blocos); `TestFeatureSwitchStopsLiveLinks` (desligar o recurso derruba os links já criados, e o apelido para de resolver sem derrubar o token); `TestDropAdmitHoldsTheLinkUntilTheSessionExists` (a admissão devolve o mutex do link ainda tomado, para conferir a cota e criar a sessão serem uma coisa só) e `TestDropQuotaHoldsAgainstParallelSessions` (o mesmo pelo protocolo: oito criações simultâneas e uma só entra); `TestSharePasswordMinimumLength` (senha de link segue o mínimo de 8 das senhas de conta); `TestInitialAdminPasswordIsGenerated` (sem `FILEZAM_ADMIN_PASSWORD` a conta inicial nasce com senha sorteada que sai no log, `admin`/`admin` é recusada e a troca segue obrigatória) | `go test ./internal/server/` |
| Sessões de upload | `internal/uploads/service_test.go` | `TestSupersedeSparesSessionReceivingChunk` (sessão com bloco em voo não é descartada por quem cria outra para o mesmo destino) e `TestSupersedeOnlyOwnSession` (sessão de outro dono, ou de outro remetente do mesmo link de recebimento, nunca é descartada) | `go test ./internal/uploads/` |
| TOTP, senhas e limites | `internal/auth/totp_test.go`, `password_test.go`, `ratelimit_test.go` | vetores do RFC 4226, janela ±1, anti-replay, códigos de recuperação de uso único, AES-GCM; `KeyedSemaphore.Acquire` espera o slot da própria chave e desiste no prazo; `TestNewReadablePassword` (forma em quatro grupos, alfabeto sem caracteres ambíguos, passa na política, 200 sorteios sem repetir) | `go test ./internal/auth/` |
| Configuração | `internal/config/config_test.go` | padrões, parsing (tamanhos, durações, proxies), valores inválidos recusados, banco dentro da raiz recusado inclusive via symlink | `go test ./internal/config/` |
| Miniaturas | `internal/thumbs/thumbs_test.go` | formatos com e sem decodificador; `Prune` respeita o teto, apaga do mais antigo e não encosta em nada quando já cabe | `go test ./internal/thumbs/` |
| Índice de nomes | `internal/index/indexer_test.go` | varredura completa sem lixeira nem partes, pesquisa (nome sem caixa nem acentos, prefixo exato, `%` literal), linhas velhas removidas por varredura/reindexação/`Touch`, uma varredura por vez | `go test ./internal/index/` |
| Migrações e índice | `internal/store/migrations_test.go` | banco só com `001` + linhas antigas → `Open` aplica as migrações seguintes sem perder dados; `TestIndexLikeEscape` (`%`/`_` literais na pesquisa); `TestUpgradeRefoldsIndex` (banco da 015 com nomes só em minúsculas é redobrado em mais de um lote ao abrir e acha nome acentuado sem varredura); `TestSlugUniqueIndexIsPartial` (apelidos vazios coexistem, duplicado conflita, revogado não volta ao pool) | `go test ./internal/store/` |
| Frontend puro | `web/src/**/*.test.ts` | `scheduler` (classify, pickBatch, backoff, chunkRange), `dropUploader` (fronteira PUT único/blocos, plano de blocos, limite do link não entra em retry), `paths` (incluindo `dirChain`: enviar uma pasta precisa avisar também as pastas acima, senão a listagem aberta fica desatualizada), `naturalSort`, `fold` (mesmos casos de `TestFold`), `updates` (comparação de versão, com a guarda do `dev`) | `cd web && npx vitest run` |
| Tipos | — | `tsc --noEmit` (inclui as chaves de `en.ts` e `es.ts` contra `pt-BR.ts`) | `cd web && npm run typecheck` |
| Cabeçalhos de licença | `scripts/license-header.sh` | todo `.go`, `.ts`, `.tsx`, `.css` e `.sh` do código tem o bloco `SPDX-License-Identifier: AGPL-3.0-only` nas primeiras linhas (migrações SQL ficam de fora) | `make headers` (`make headers-apply` corrige) |

`make test` roda tudo. Um único teste Go: `go test ./internal/vfs/ -run TestEscapeAttemptsAreRefused -v`. Sem Go instalado: `docker run --rm -v "$PWD":/src -w /src -e GOFLAGS=-buildvcs=false golang:1.26-alpine go test ./...`.

A CI do GitHub (`.github/workflows/ci.yml`) roda em cada push e PR: cabeçalhos de licença, `tsc`, vitest, build do Vite, `npm audit --omit=dev`, `go vet`, `go test ./...`, build do binário, `govulncheck` e o build da imagem Docker. `TestShareBoundToInode` depende de o sistema de arquivos temporário **não** reutilizar o número de inode de uma pasta recém-apagada: passa em ext4 e tmpfs, falha em overlayfs (Docker sem `--tmpfs /tmp:exec`).

Os testes de integração criam raiz e banco temporários; nada toca o sistema real. `db.Now` é injetável para testar expirações.

## O que não está coberto automaticamente

- Interação real do navegador (drag-and-drop, teclado, virtualização). Verificado manualmente com Chrome headless via DevTools Protocol (scripts em `scratchpad` na sessão de desenvolvimento; não versionados).
- Comportamento atrás de proxies reais, no Cloudflare e no Easypanel.
- Sistemas de arquivos específicos (foi validado em ext4 e ntfs3).

## Checklist manual antes de uma versão

1. `docker compose up` limpo, **sem `.env`** → a senha do admin sai sorteada no log, uma vez → login com ela → troca obrigatória → senha antiga recusada. Conferir que `admin`/`admin` não entra.
2. Upload de arquivo de vários GB: memória do container estável (`docker stats`), recarregar no meio e retomar soltando o mesmo arquivo, `sha256sum` igual.
3. Pasta com 10 mil arquivos pequenos: UI responsiva, contagem correta.
4. Copiar/mover/excluir árvore grande com progresso e cancelamento.
5. ZIP de pasta grande abre com `unzip`. Pasta acima de 2 GB mostra as partes; extrair todas na mesma pasta dá o conteúdo inteiro (compare com `diff -r`).
6. Link de 1 hora em janela anônima; revogar → 404.
7. `curl '.../api/files?path=../../etc/passwd'` → 400; symlink em `/data` para `/config` não abre.
8. `evil.svg` com `<script>` e `evil.html`: sem alerta, HTML só como texto/download.
9. Usuário com escopo não vê o pai; admin muda o escopo → sessão cai.
10. Atrás do proxy real: cookie `Secure`, IP real na auditoria, upload de 200 MB completa.
11. Preview de PDF abre dentro da interface (Chrome e Firefox). No celular (Android e iPhone) aparece "Abrir em nova aba"/"Baixar" e a nova aba mostra o PDF inteiro. Abrir um PDF, fechar pelo X e apertar `Delete`: pede para mover o arquivo para a lixeira (o mesmo depois de cancelar um F2).
12. `.md` com tabela, lista de tarefas, imagem relativa (`img/x.png`), `<script>`/`<img onerror>` e `[x](javascript:alert(1))`: abre formatado, imagem aparece, nenhum alerta, o HTML some e o link perigoso vira texto; "Texto" mostra o fonte. Também pelo link público de pasta, com várias imagens: todas aparecem (nenhum 429 na aba Rede).
13. No celular (ou DevTools em modo touch, 390 px): toque abre, toque longo abre o menu e seleciona, "⋯" mostra as ações, tabelas cabem na largura; tema claro/escuro e cores em Configurações refletem na hora.
14. Painel de upload: cancelar itens e cancelar tudo não cria barra de rolagem horizontal na lista (status "Cancelado" é mais largo que a porcentagem). Soltar uma pasta grande e parar o servidor no meio: "Só os não enviados" isola as falhas, "Copiar lista" cola caminho e motivo, o CSV abre no Excel/LibreOffice com acentos e colunas certas. Fechar a aba com envio em andamento (também pausado e no link de recebimento) pede confirmação; depois de terminar, não pede.
15. **Editor**: abrir um `.md`, editar, a prévia acompanha, salvar, conferir no disco. O mesmo arquivo em duas abas: a segunda recusa com `modified` e o texto digitado continua na tela. Arquivo acima de 1 MiB → opção desabilitada.
16. **Extrator**: extrair um `.zip` normal; extrair de novo cria `nome (1)`. Um zip forjado com `../`, symlink e `.filezam-*` não tira nada do lugar. Um `.zip` com senha (`zip -e` ou 7-Zip com AES) é recusado na hora com a mensagem de arquivo protegido e nenhuma pasta aparece. Cancelar no meio não deixa pasta pela metade. Compactar uma seleção e abrir o `.zip` fora do Filezam.
17. **Miniaturas**: pasta com fotos em grade; a segunda visita revalida com `304` (aba Rede); desligar em Configurações volta ao ícone na hora. Abrir uma foto e passar com as setas numa conexão lenta (DevTools, 3G): a foto anterior some na hora, entra a miniatura desfocada com o indicador, e a vizinha já pré-carregada aparece sem espera. PNG com dimensões absurdas → ícone, sem estourar memória.
18. **Link com apelido**: criar exige senha de 8 caracteres; o link travado não mostra o nome da pasta; revogar não devolve o apelido, e `?purge=1` devolve.
19. **Link de recebimento**: criar aponta para pasta vazia criada na hora, com cota e vencimento obrigatórios (máx. 30 dias). Enviar de uma janela anônima, vários arquivos ao mesmo tempo e um grande em blocos: todos aparecem na lista de quem enviou e no disco do dono; um nome repetido vira `nome (1)`; ler pelo link responde 404. Atrás do proxy real, com uma conexão lenta de verdade: um envio grande não segura os outros nem estoura o tempo limite do proxy.
20. **Notificações**: enviar arquivos por um link de recebimento de uma janela anônima com o dono logado em outra aba: em até 30 s aparece o aviso, o contador no menu e a notificação somando os arquivos; "Abrir pasta" leva à pasta e marca como lida. **Configurações do sistema**: os quatro interruptores desligam o recurso para links e menus que já existem, não só na criação.
21. `go run golang.org/x/vuln/cmd/govulncheck@latest ./...` sem vulnerabilidades alcançáveis e `cd web && npm audit --omit=dev` limpo; se o Go tiver correção nova, subir `go.mod` e `Dockerfile`.
22. Com `FILEZAM_METRICS_TOKEN`: `curl -H 'Authorization: Bearer …' /metrics` responde e o Prometheus consegue coletar; sem token → 404.
23. Ativar 2FA com um app real (Aegis/Google Authenticator), sair, entrar com código, marcar "confiar", sair e entrar sem código; usar um código de recuperação; desativar.
24. Reiniciar o container no meio de uma cópia grande de vários arquivos: a operação aparece em **Operações** como interrompida com "cópia parcial", os arquivos que terminaram ficam e não sobra nenhum `.filezam-copy-*` no destino. Com um arquivo só, nada fica no destino. Cancelar pela interface mostra o mesmo aviso no job.
25. **Painel** (Administração → Painel): com outro usuário enviando uma pasta de fotos, ele aparece em "Enviando agora" com arquivos e bytes subindo a cada 15 s, e some uns 2 min depois de terminar; um arquivo grande aparece com progresso em "Arquivos grandes"; um envio por link de recebimento aparece em nome do link. A cota perto do fim gera alerta; "Encerrar" a sessão de outra pessoa a desloga na hora e aparece na Auditoria. No celular (390 px) a página não rola na horizontal.
26. **Nomes que só diferem na caixa** (HD NTFS real): numa pasta com `video.mp4`, enviar `VIDEO.MP4` pergunta na hora dizendo com que nome ele já está lá. "Substituir" deixa um arquivo só, `video.mp4`, com o conteúdo novo; "Manter ambos" cria `VIDEO (1).MP4`. Pelo Samba/Windows a pasta não mostra dois itens iguais. Enviar a pasta `fotos/` para onde já existe `Fotos/` junta tudo em `Fotos/`.
27. **Mover para…**: selecionar arquivos e pastas, "Mover", navegar até outra pasta (inclusive criando uma pelo próprio diálogo) e confirmar: os itens saem da origem e chegam ao destino, com a pergunta de conflito quando o nome já existe lá e o aviso de link público que cai. A pasta que está sendo movida aparece desabilitada no seletor, e o botão não libera enquanto o destino for a pasta de origem. No celular, o mesmo pelo "⋯". **Coluna Tipo**: a listagem mostra `PDF`, `JPG`, `Pasta`; clicar no cabeçalho agrupa por tipo com as pastas primeiro, e a ordem se mantém ao rolar uma pasta paginada (mais de 2000 itens), onde quem ordena é o servidor.
28. **Aviso de atualização**: com uma aba aberta na listagem, subir uma versão nova do servidor — em até 30 s (a consulta de não lidas) a faixa "O Filezam foi atualizado para a versão X.Y.Z" aparece sozinha, sem recarregar nada; "Recarregar" traz a interface nova (conferir o número no rodapé dos créditos). Com um envio em andamento, o editor aberto, um diálogo na tela ou arquivos recortados esperando colar, trocar de aba **não** recarrega sozinho; sem nada disso, trocar de aba e voltar mostra a versão nova sem ter visto a faixa. No `make dev` a faixa nunca aparece (o binário se diz `dev`).
